function im = load_image(name)

    I = imread(name);
    [h, w, c] = size(I);
    dim = 256;
%     if(h<w)
%         I = imresize(I, [dim, NaN]);
        I = imresize(I, [dim, dim]);
%     else
%         I = imresize(I, [NaN, dim]);
%     end
%     I = single(rgb2gray(I));
    I = single(I);
%     I = I - mean(mean(I));

%     m = min(min(I));
%     M = max(max(I));
%     im = (I-m)/(M-m);
    im = I;
    
%     gx = filter2([-1 0 1], im, 'same');
%     gy = filter2([-1 0 1]', im, 'same');
%     im = gx.*gx+gy.*gy;
%     m = min(min(im));
%     M = max(max(im));
%     im = 2*(im-m)/(M-m)-1;
end